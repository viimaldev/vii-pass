{
  "name": "vii-pass-marketing-video",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Isolated media pipeline for the Vii Pass marketing video (spec 022). NOT an npm workspace — never imported by app code.",
  "scripts": {
    "seed": "node seed-demo.mjs",
    "voice": "node --use-system-ca generate-voiceover.mjs",
    "record": "node record-demo.mjs",
    "assemble": "node assemble-video.mjs"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.2.0",
    "msedge-tts": "^2.0.7",
    "playwright": "^1.45.0"
  }
}
