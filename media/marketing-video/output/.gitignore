# Per-scene intermediates (WebM clips, per-scene MP3s, durations.json) are
# regenerated by the pipeline — only the final deliverables are committed.
scenes/
# Local backups of previous voice takes (not deliverables).
*.bak.*
